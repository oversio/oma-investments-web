export enum SettingsQueryKey {
  all = "all",
  settings = "settings",
  companyTypes = "company-types",
  dividendTypes = "dividend-types",
  indicators = "indicators",
  securityLevels = "security-levels",
  thresholdSettings = "threshold-settings",
}
